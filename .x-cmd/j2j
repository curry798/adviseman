
f(){
    local dst; local line
    cd $(x wsroot)
    find src -name '*.json' | while read -r line; do
        dst="dst${line#src}"
        dst="${dst%.json}.t.jso"
        x ensurefp "$dst"
        x ja t < "$line" > "$dst"
    done
}

f