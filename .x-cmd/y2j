
cd $(x wsroot)
find src -name advise.yml | while read -r line; do
    dst="dst${line#src}"
    dst="${dst%.yml}.t.json"
    mkdir -p "${dst%/*}"
    x yml y2j "$line" | x ja t > "$dst"
    # echo $line $dst
done

